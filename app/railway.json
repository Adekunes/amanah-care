{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile.fly" },
  "deploy": { "restartPolicyType": "ALWAYS", "healthcheckPath": "/api/health", "healthcheckTimeout": 120 }
}
